# 发布前检查清单

- [ ] 确认目标仓库 URL，或确认需要新建的 `账号或组织/仓库名`。
- [ ] 确认仓库先设为 private 还是立即 public。
- [ ] 明确授权在本独立发布目录初始化 Git、创建提交并 push。
- [ ] 确认作者、单位、ORCID、联系邮箱、论文题目/DOI和推荐引用。
- [ ] 确认许可；建议数据与模型 CC BY 4.0、项目自有 Python 代码 MIT，但不得代替作者决定。
- [ ] 核验生产 GPUMD 的 tag/commit、源码修改状态、编译器/CUDA 和可执行文件 SHA-256。
- [ ] 决定是否本轮同时加入代表性 `model.xyz`、`run.in` 和论文源数据。
- [ ] 推送后从远端重新 clone，复核目录树、文件大小和 `SHA256SUMS.txt`。

当前 GitHub CLI 已安装但尚未登录。`D:\AI` 本地仓库没有远端，且范围过宽；不要把它整体绑定并推送。本候选应作为独立仓库根目录。

